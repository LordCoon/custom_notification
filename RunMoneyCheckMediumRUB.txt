include("include/SOVIETInstructions.txt");
include("include/checkMoney.txt");
include("config.txt");

defineVariable(int, _scriptRunning_moneyCheckMediumRUB);

defineFunction(main, void)
{
	InitConstants();
	InitConfig();

	while( 1 ? 1)
	{
		Script_Exists("moneyCheckMediumRUB.txt", _scriptRunning_moneyCheckMediumRUB);

		if(!(_scriptRunning_moneyCheckMediumRUB ? 1))
		{
			Script_StartNew("moneyCheckMediumRUB.txt");
		}

        Script_Sleep(10.0);
	}

	Window_ShowText("Script Ended because of error.");
	
	end();
}