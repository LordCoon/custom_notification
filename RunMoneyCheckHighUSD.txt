include("include/SOVIETInstructions.txt");
include("include/checkMoney.txt");
include("config.txt");

defineVariable(int, _scriptRunning_moneyCheckHighUSD);

defineFunction(main, void)
{
	InitConstants();
	InitConfig();

	while( 1 ? 1)
	{
		Script_Exists("moneyCheckHighUSD.txt", _scriptRunning_moneyCheckHighUSD);

		if(!(_scriptRunning_moneyCheckHighUSD ? 1))
		{
			Script_StartNew("moneyCheckHighUSD.txt");
		}
		
        Script_Sleep(10.0);
	}

	Window_ShowText("Script Ended because of error.");
	
	end();
}